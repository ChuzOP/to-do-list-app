import { createTheme } from "@mui/material/styles";

const ThemeLight = createTheme({
  palette: {
    background: {
      default: "#242d40",
    },
    text: {
      primary: "#DBDFFD",
    },
    primary: {
      main: "#3624a6",
    },
    secondary: {
      main: "#C9B6E4",
    },
  },
});

export default ThemeLight;
